@model TestViewModel
@{
    ViewData["Title"] = "Home Page";
}
<div>@ViewBag.Test @this.ViewContext.RouteData.Values["culture"]</div>
<div>@Html.ActionLink("Home","Index",new{culture=this.ViewContext.RouteData.Values["culture"]})</div>
<form asp-controller="Home" asp-action="Index" method="post">
    @Html.ValidationSummary()
    @Html.EditorForModel()
    <br />
    @*   @Html.EditorFor(o=>o.Sub)
    *@
    @if (Model != null)
    {
        for (var i = 0; i < Model.Values1.Count; i++)
        {
            @Html.EditorFor(o => Model.Values1[i])
        }
            
        for (var i = 0; i < Model.Values2.Count; i++)
        {
            @Html.EditorFor(o => Model.Values2[i])
        }
    }
    <button type="submit">Submit</button>
</form>
